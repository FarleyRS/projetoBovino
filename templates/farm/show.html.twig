{% extends 'base.html.twig' %}

{% block title %}Farm{% endblock %}

{% block body %}
    <h1>Farm</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ farm.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ farm.nome }}</td>
            </tr>
            <tr>
                <th>Tamanho</th>
                <td>{{ farm.tamanho }} ha</td>
            </tr>
            <tr>
                <th>Responsavel</th>
                <td>{{ farm.responsavel }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_farm_index') }}">back to list</a>

    <a href="{{ path('app_farm_edit', {'id': farm.id}) }}">edit</a>

    {{ include('farm/_delete_form.html.twig') }}
{% endblock %}
