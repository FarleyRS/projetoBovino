<form method="post" action="{{ path('app_cow_delete', {'id': cow.id}) }}" onsubmit="return confirm('Realmente deseja deletar esse item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cow.id) }}">
    <button class="btn">Deletar</button>
    <a class = "btn" href="{{ path('app_cow_edit', {'id': cow.id}) }}">Editar</a>
    <a class = "btn" href="{{ path('app_cow_index') }}">Voltar a lista</a>
</form>
