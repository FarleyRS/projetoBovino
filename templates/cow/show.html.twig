{% extends 'base.html.twig' %}

{% block title %}Cow{% endblock %}

{% block body %}
    <h1>Cow</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cow.id }}</td>
            </tr>
            <tr>
                <th>Codigo</th>
                <td>{{ cow.codigo }}</td>
            </tr>
            <tr>
                <th>Qt_leite</th>
                <td>{{ cow.qtLeite }}</td>
            </tr>
            <tr>
                <th>Qt_racao</th>
                <td>{{ cow.qtRacao }}</td>
            </tr>
            <tr>
                <th>Peso</th>
                <td>{{ cow.peso }}</td>
            </tr>
            <tr>
                <th>Nascimento</th>
                <td>{{ cow.nascimento ? cow.nascimento|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cow_index') }}">back to list</a>

    <a href="{{ path('app_cow_edit', {'id': cow.id}) }}">edit</a>

    {{ include('cow/_delete_form.html.twig') }}
{% endblock %}
